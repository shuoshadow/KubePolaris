import React from 'react';
import { Card, Typography } from 'antd';

const { Title } = Typography;

const PodDetail: React.FC = () => {
  return (
    <div>
      <Title level={2}>Pod详情</Title>
      <Card>
        <p>Pod详情页面正在开发中...</p>
      </Card>
    </div>
  );
};

export default PodDetail;